<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <queries>
        <package android:name="com.qwulivoice.beta" />
        <package android:name="com.qwulivoice.beta.debug" />
        <provider android:authorities="com.qwulivoice.beta.config" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@drawable/qwulivoice_avatar"
        android:label="@string/app_name"
        android:roundIcon="@drawable/qwulivoice_avatar"
        android:supportsRtl="true"
        android:theme="@style/Theme.Voicechanger.Companion">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
